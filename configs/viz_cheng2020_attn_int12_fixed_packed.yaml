experiment:
  name: viz_cheng2020_attn_int12_fixed_packed
  seed: 1234

model:
  source: compressai
  name: cheng2020-attn
  quality: 3
  metric: mse
  pretrained: true

data:
  dataset: kodak_viz
  root: data/kodak_viz
  batch_size: 1
  num_workers: 0

precision:
  mode: int12_fixed_packed
  method: fixed_point_activation_codec
  fixed_point_bits: 12
  fractional_bits: null
  storage_mode: packed
  activation_modules:
    - g_a
    - g_s
    - h_a
    - h_s

evaluation:
  metrics:
    - bpp
    - psnr
    - ms_ssim
  benchmark_forward: false
  benchmark_memory: true
  save_reconstructions: true

output:
  dir: results/visualizations/cheng2020_attn_int12_fixed_packed
